@model ProductDetailsServiceModel

@{
    ViewBag.Title = "Product Details";
}

@{
    var borrowerName = "Noone is currently borrowing this product!";

    if (Model.BorrowerName != null)
    {
        borrowerName = Model.BorrowerName;
    }
}

@*<div class="card" style="width: 18rem; outline-offset: 10px">
    <img class="card-img-top" style="width: 100%; height: 10vw; object-fit: scale-down;" src="@Model.ImageUrl" alt="@Model.Brand @Model.Name">
    <div class="card-body col-sm-12 col-md-12 col-lg-12" style="height: 10vw">
        <h5 class="card-title">@Model.Brand @Model.Name</h5>
        <p class="card-text d-sm-none d-md-block">@Model.Description</p>
    </div>
    <ul class="list-group list-group-flush">
        <li class="list-group-item">Category: @Model.CategoryName </li>
        <li class="list-group-item">Location: @Model.Location</li>
        <li class="list-group-item">Lending price: @Model.LendingPrice lv.</li>
        <li class="list-group-item">Owner: @Model.LenderName </li>
        <li class="list-group-item">Current borrower: @Model.BorrowerName</li>
    </ul>
</div>*@


<section class="mb-5">

    <div class="row">
        <div class="col-md-6 mb-4 mb-md-0">

            <div id="mdb-lightbox-ui"></div>

            <div class="mdb-lightbox">

                <div class="row product-gallery mx-1">

                    <div class="col-12 mb-0">
                        <figure class="view overlay rounded z-depth-1 main-img">
                            <a href="@Model.ImageUrl"
                               data-size="710x823">
                                <img src="@Model.ImageUrl"
                                     class="img-fluid z-depth-1">
                            </a>
                        </figure>
                    </div>
                </div>
            </div>

        </div>
        <div class="col-md-6">

            <h5>@Model.Brand.ToUpper() @Model.Name</h5>

            <p><span class="mr-1"><strong>Price per day: @Model.LendingPrice lv.</strong></span></p>
            <p class="pt-1">@Model.Description</p>
            <div class="table-responsive">
                <table class="table table-sm table-borderless mb-0">
                    <tbody>
                        <tr>
                            <th class="pl-0 w-25" scope="row"><strong>Category</strong></th>
                            <td>@Model.CategoryName</td>
                        </tr>
                        <tr>
                            <th class="pl-0 w-25" scope="row"><strong>Location</strong></th>
                            <td>@Model.Location</td>
                        </tr>
                        <tr>
                            <th class="pl-0 w-25" scope="row"><strong>Owner</strong></th>
                            <td>@Model.LenderName</td>
                        </tr>
                        <tr>
                            <th class="pl-0 w-25" scope="row"><strong>Current borrower</strong></th>
                            <td>@borrowerName</td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <hr>

            <button type="button" class="btn btn-primary btn-md mr-1 mb-2">Lend now</button>
        </div>
    </div>
</section>